set number "显示行号
set cursorline "突出显示当前行
set nocompatible "关闭vi兼容模式
set tabstop=2 "设置tab长度为2
set autochdir "自动切换工作目录为当前路径
set ignorecase smartcase " 搜索时忽略大小写，但在有一个或以上大写字母时仍保持敏感
set hlsearch " 搜索时高亮被找到的文本
set matchtime=2 "跳转到匹配括号的时间
set magic
set smartindent "新行智能缩进



